// Массивы

arr.toLowerCase();

arr.forEach(function (item, i, arr) {

});

arr.filter();

// DOM
document.querySelector('XXX');

document.createElement('XXX');

parent.append(child); // add child element into parent element

// События

addEventListener('click', () => {  // вызов функции в виде function(); сразу вызовет её

});

// Прочее

const variable = elem.classList; // возвр список классов элемента

elem.classList.add('name of a class'); // добавить класс элементу

var text = element.textContent;
element.textContent = "Это просто текст";
    // Позволяет задавать или получать текстовое содержимое элемента и его потомков.